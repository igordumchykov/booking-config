spring:
  application:
    name: apigateway
eureka:
  client:
    serviceUrl:
      defaultZone: http://myhost:8761/eureka/
zuul:
  prefix: /api
  routes:
    search-service:
      serviceId: search-service
      path: /flights/**

